Barikade Server (Render)

Deploy:
- New Web Service (Node)
- Build Command: npm install
- Start Command: npm start

WebSocket URL (client uses):
wss://barikade-server.onrender.com

Health check:
GET /health
